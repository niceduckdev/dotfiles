# prompt
PS1='\u@\H -> '

# aliases
alias ls="eza -l -a"
alias vim="nvim"
alias stow="stow --ignore='\.nix$'"

# fix bash opening in random directories
cd /home/kaj

# set neovim as default editor
export EDITOR "nvim"
export VISUAL "nvim"
